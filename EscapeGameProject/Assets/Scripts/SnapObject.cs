using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SnapObject : MonoBehaviour
{

    void Start()
    {
        
    }


    void Update()
    {
        
    }


/*    public GameObject wallPrefab; // 벽 프리팹을 Inspector에서 설정

    public void BuildWall()
    {
        // 선설 오브젝트의 위치와 방향을 얻습니다.
        Vector3 constructionPosition = transform.position;
        Quaternion constructionRotation = transform.rotation;

        // 벽 프리팹을 선설 위치와 방향에 생성합니다.
        GameObject wall = Instantiate(wallPrefab, constructionPosition, constructionRotation);

        // 생성된 벽을 선설 오브젝트의 자식으로 만들어 정확한 위치를 유지합니다.
        wall.transform.SetParent(transform);
    }*/
}
